<h1>Activity Signup</h1>

<%= form_for @signup do |f| %>
<%= f.label :activity %>
<%= f.collection_select :activity_id, Activity.all, :id, :name %>
<%= f.label :camper %>
<%= f.collection_select :camper_id, Camper.all, :id, :name %>
<%= f.label :time %>
<%= f.number_field :time %>
<%= f.submit %>
  
<% end %>
<p>Find me in app/views/signups/new.html.erb</p>
